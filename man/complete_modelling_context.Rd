% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modelling_context.R
\name{complete_modelling_context}
\alias{complete_modelling_context}
\title{Complete a Modelling Context with Additional Regressors}
\usage{
complete_modelling_context(
  modelling_context,
  y,
  group = "r",
  name = "",
  overwrite = FALSE
)
}
\arguments{
\item{modelling_context}{A modelling context object created by
\code{\link{modelling_context}}. It's a list of calendars and variables.}

\item{y}{A regressor. An object to format (\code{mts}, \code{ts}, \code{JD3_TS},
\code{JD3_DYNAMICTS}, \code{JD3_TSCOLLECTION}, \code{list}, or other).}

\item{group}{Character. Name of the group to which the variable should be
added. By default, the group is "r".}

\item{name}{Character. Name to assign to the new regressor variable.}

\item{overwrite}{a Boolean to indicate whether a variable already present
should be replaced}
}
\value{
The updated modelling context with the new variable added.
}
\description{
Adds a new regressor variable to an existing modelling context,
either creating a new group or adding to an existing group.
}
\details{
If a variable with the same name already exists in the group and overwrite
is \code{FALSE}, returns the original modelling context with a message.
}
\examples{
\dontshow{if (rjd3jars::check_java_version(silent = TRUE)) withAutoprint(\{ # examplesIf}
# Assuming we have a modelling context and a new regressor
my_context <- modelling_context()

# Add the new regressor to the context
my_context <- complete_modelling_context(
    modelling_context = my_context,
    y = AirPassengers,
    group = "my_group",
    name = "my_regressor"
)

# Add another regressor to the same group with same name
my_context <- complete_modelling_context(
    modelling_context = my_context,
    y = AirPassengers,
    group = "my_group",
    name = "my_regressor"
)

# Add another regressor to the same group
my_context <- complete_modelling_context(
    modelling_context = my_context,
    y = ABS,
    group = "my_group",
    name = "another_regressor"
)
\dontshow{\}) # examplesIf}
}
